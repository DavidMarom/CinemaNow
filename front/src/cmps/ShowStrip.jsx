import React from 'react'
import { useDispatch } from "react-redux";
import { addMov, removeMov } from '../store/actions/movActions'
import history from '../history';

export const ShowStrip = () => {
    // console.log(mov);
    // const dispatch = useDispatch();

    return (
      <div></div>
    )
}
